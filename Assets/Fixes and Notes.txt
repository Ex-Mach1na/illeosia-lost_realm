#1: 
In World.cs
	In GetVoxel
		Noise.Get2DPerlin(new Vector2(pos.x, pos.z), 12345, 2f) > 0.5f 
		this is used to select index. randomize it using another function to get random value. Make more biomes.
		Get index values.
#2:

